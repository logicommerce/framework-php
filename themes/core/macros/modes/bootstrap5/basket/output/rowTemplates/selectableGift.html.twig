{% macro productSelectableGift(args, basketRow) %}
    
    {% if args.productSelectableGiftTemplate != null %}
    {% else %}{# default template #}
    {% endif %}{# end default template #}

{% endmacro productSelectableGift %}